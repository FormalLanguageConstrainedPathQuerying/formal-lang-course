from networkx import MultiDiGraph
from pyformlang.cfg import CFG, Variable


def cfpq_all(cfg: CFG, graph: MultiDiGraph):
    return None


def cfpq(
    cfg: CFG,
    graph: MultiDiGraph,
    start_nodes: set = None,
    final_nodes: set = None,
    start_symbol: Variable = Variable("S"),
):
    return None
